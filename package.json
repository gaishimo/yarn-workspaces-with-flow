{
  "name": "yarn-workspace-flow",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "flow": "flow",
    "clean": "rm -rf node_modules modules/*/node_modules modules/*/dest"
  },
  "devDependencies": {
    "flow-bin": "^0.76.0"
  }
}
